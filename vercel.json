{
  "framework": "sveltekit",
  "installCommand": "bun install --ignore-scripts",
  "buildCommand": "bunx prisma generate && bunx svelte-kit sync && bun run build",
  "ignoreCommand": "if [[ \"$VERCEL_GIT_COMMIT_MESSAGE\" == *'[skip ci]'* ]] || [[ \"$VERCEL_GIT_COMMIT_MESSAGE\" == chore\\(release\\):* ]]; then exit 0; else exit 1; fi"
}
