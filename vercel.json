{
  "framework": "sveltekit",
  "installCommand": "bun install --ignore-scripts",
  "buildCommand": "bunx prisma generate && bunx svelte-kit sync && bun run build",
  "ignoreCommand": "echo \"$VERCEL_GIT_COMMIT_MESSAGE\" | grep -q '\\[RELEASE\\]' && exit 1 || exit 0"
}
